# Real Provider Stability and Log UI Report

## Timestamp
- Date: 2026-05-12 (Asia/Saigon)

## Scope
- Validate real Hub lifecycle paths for:
  - `pdf-to-podcast`
  - `agentic-commerce-blueprint`
  - `multi-agent-intelligent-warehouse`
- Use the frontend/Hub install path backed by real provider scripts. No smoke-only path, no `dryRun`, and no local source fallback.
- Fix provider wrapper or upstream provider issues that block future GitHub clones.
- Split provider logs into a concise progress view and a detailed log view.

## What Changed
- Hub provider detail now has two log tabs:
  - `Progress`: current task step, runtime status, health, and recent concise messages.
  - `Detailed logs`: full provider log stream with level filters.
- `pdf-to-podcast` Windows run/stop wrappers now start Git Bash provider scripts in isolated processes, wait for a complete generated port map, and verify both API and Gradio frontend health before marking the provider running.
- `multi-agent-intelligent-warehouse` Windows stop wrapper now handles partial deploy directories. If compose files are missing, stop/delete records a stopped state instead of failing the Hub task.
- README now lists all three actively verified provider integrations.

## Upstream Provider Fixes
- `https://github.com/PhuongHo03/pdf-to-podcast.git`
  - `3c7e7af fix(setup): prefer venv python for port detection`
  - `4e9ffb6 fix(setup): detach frontend process on windows bash`

The final Hub install for `pdf-to-podcast` cloned commit `4e9ffb6` from GitHub and passed the real lifecycle.

## Real Lifecycle Validation
- `pdf-to-podcast`
  - Source: `https://github.com/PhuongHo03/pdf-to-podcast.git`
  - Commit: `4e9ffb6`
  - Result: `delete -> install -> run -> status/logs/stop/delete` passed.
  - Runtime check: frontend port `7860`, script health `ok`, API health `ok`.
- `agentic-commerce-blueprint`
  - Source: `https://github.com/baolnq-ai/Agentic-Commerce-blueprint-provider-`
  - Commit: `426454f`
  - Result: `delete -> install -> run -> status/logs/metrics/stop/delete` passed.
  - Runtime check: gateway health `ok` on port `8088`.
- `multi-agent-intelligent-warehouse`
  - Source: `https://github.com/baolnq-ai/Multi-Agent-Intelligent-WarehousePublic-nvidia`
  - Commit: `4729d72`
  - Result: `delete -> install -> run -> status/logs/metrics/stop/delete` passed.
  - Runtime check: frontend port `3001`, backend API health `ok` on port `8091`, DB migrations and default user login check passed.

## Verification
- Backend:
  - `ruff check .`
  - `ruff format --check .`
  - `mypy app`
  - `python scripts/validate_providers.py`
  - `python scripts/check_no_secrets.py`
  - `pytest --cov=app --cov-report=term-missing --cov-fail-under=80`
  - `python scripts/provider_dry_run_lifecycle.py`
  - `python scripts/benchmark_latency.py --iterations 100 --warmup 20 --threshold-ms 100`
  - OpenAPI smoke check
- Frontend:
  - `npm run typecheck`
  - `npm run test`
  - `npm run build`
- Provider scripts:
  - PowerShell parser check for root/provider `.ps1` scripts.
  - Git Bash syntax check for root/provider `.sh` scripts.

## Cleanup
- Removed provider `deploy/` clones after validation.
- Removed provider runtime/log files generated by real tests while keeping `.gitkeep`.
- Removed frontend build output and backend cache/test artifacts.

## Follow-up Gate
- After this report is pushed, the repository must be cloned fresh from GitHub, `.env.local` restored locally, setup run again, and all three real provider lifecycles repeated from clean clone state.
